<br />
<br />
<% @sites.each do |site| %>
  <div class="row">
    <div class="col s12">
      <div class="card">
        <div class="card-content">
          <span class="card-title"><%= site[:title] %></span>
          <p><%= site[:status] %></p>
        </div>
      </div>
    </div>
  </div>
<% end %>

<a href="/">Back to search</a>
